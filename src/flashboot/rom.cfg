MEMORY {
    ZP:       file = "", define = yes, start = $000C, size = $0084;
    ROMINFO:  file = %O,               start = $a000, size = $0018;
    MAIN:     file = %O, define = yes, start = $a018, size = $1fee;
    DATA:     file = "", define = yes, start = $2000, size = $2000;
}

SEGMENTS {
    ZEROPAGE: load = ZP,       type = zp;
    ROMINFO:  load = ROMINFO,  type = ro;
    CODE:     load = MAIN,     type = ro;
    PRGEND:   load = MAIN,     type = ro;
    DATA:     load = DATA,     type = rw;
}
