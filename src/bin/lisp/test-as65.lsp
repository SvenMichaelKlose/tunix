(load 'as65.lsp)
(load 'as65-parse.lsp)
(or (equal (as65-parse '(label:))
           '(nil (label . label)))
    (error))
(or (equal (as65-parse '(label :))
           '(nil (label . label)))
    (error))
(or (equal (as65-parse '(lda # 0))
           '(nil (mode . imm) (op . 0)))
    (error))
(or (equal (as65-parse '(lda 1234,x))
           '(nil (absr . x) (op . 1234) (mnem . lda)))
    (error))
(or (equal (as65-parse '(lda (1234),y))
           '(nil (absr . y) (op 1234) (mode . ind) (mnem . lda)))
    (error))

(message "as65 tests passed.")
