CC			= zcc
CFLAGS      = +$(TARGET) -c -Werror -Wall -DTARGET=$(TARGET) -I$(TUNIX_LIB)
LD		    = zcc
LDFLAGS     = +$(TARGET) -b
NO_EXOMIZER = 1
AR		    = zcc
ARFLAGS		= +$(TARGET)

ifdef NDEBUG
COPTFLAGS  += -SO3
else
COPTFLAGS  += -SO0
endif
