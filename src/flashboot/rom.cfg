MEMORY {
    ZP:       file = "", define = yes, start = $0000, size = $0090;
    ROMINFO:  file = %O,               start = $a000, size = $000c;
    MAIN:     file = %O, define = yes, start = $a00c, size = $1ff4;
}

SEGMENTS {
    ZEROPAGE: load = ZP,       type = zp;
    ROMINFO:  load = ROMINFO,  type = ro;
    CODE:     load = MAIN,     type = ro;
    PRGEND:   load = MAIN,     type = ro;
}
